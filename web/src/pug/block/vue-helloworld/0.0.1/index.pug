span.outer
  | Vue: {{message}}
  script: :lsc
    module.exports =
      pkg:
        dependencies: [
          "https://cdn.jsdelivr.net/npm/vue@2/dist/vue.js"
        ]
      init: ({root, context}) ->
        {Vue} = context
        app = new Vue do
          el: root
          data: message: "hello world!"

