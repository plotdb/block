div
  div(plug="base") this is the content from child a
  script(type="@plotdb/block"): :lsc
    module.exports =
      pkg: extend: name: "base"
      init: ({root}) -> root.classList.toggle \d-none, false
